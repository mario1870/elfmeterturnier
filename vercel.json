{
    "version": 2,
    "builds": [
      { "src": "src/**", "use": "@vercel/next" },
      { "src": "server/**", "use": "@vercel/node" }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "src/$1" },
      { "src": "/api/(.*)", "dest": "server/$1" }
    ]
  }
  